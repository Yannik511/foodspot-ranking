# AI Images Setup (derzeit nicht aktiv)

Für die aktuelle Version der App werden keine KI-generierten Bilder mehr verwendet. Dieses Dokument stammt aus einem früheren Prototyp und bleibt lediglich als Referenz bestehen.













