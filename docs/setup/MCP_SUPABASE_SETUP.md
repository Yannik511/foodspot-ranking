# MCP Supabase Setup (Archiv)

Dieses Dokument beschrieb, wie Supabase-Zugriffe über das Model Context Protocol (MCP) erfolgen. Da MCP aktuell nicht mehr Bestandteil des Stacks ist, wird diese Anleitung nur aus historischen Gründen aufbewahrt.

Kurzfassung Supabase (heute relevant):
- Supabase Projekt wie in `../README.md` bzw. `../../README.md` beschrieben anlegen.
- Environment Variablen (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) in `.env` setzen.
- Migrationen über Supabase SQL Editor oder CLI ausführen (siehe `../../migrations/README.md`).







